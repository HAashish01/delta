from datetime import datetime , timedelta
noww = datetime.now()
after = timedelta(days=30)
print("Your registration will be expired in :", noww + after)

a = datetime.date(may: 12) -> _
datetime.date()